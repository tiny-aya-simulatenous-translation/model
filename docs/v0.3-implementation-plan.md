# v0.3 Training — Implementation Plan (PR #10)

**Target PR:** #10. **Operationalizes** the Phase A–E strategy from
[`docs/next-v0.3-training-plan.md`](next-v0.3-training-plan.md) (PR #9) into
concrete, file:function-level code work. Same principles (phased, table-driven,
EXA-grounded) — but this is the **engineering blueprint** + execution order, not
the rationale (see #9 / its References for the *why* and citations).

**Goal:** ship `tr-hi-s2st-v0.3` where **validation improves over training**, deep
codebooks learn, and both streams stay healthy — by *implementing* the levers #9
identified.

## What already exists (so we build, not rebuild)

| Capability | Where | Status for v0.3 |
|---|---|---|
| Linear-warmup → **cosine decay** LR (`min_lr_ratio`) | `src/training/scheduler.py` | ✅ reuse; set `min_lr_ratio=0` |
| **Per-codebook** CE loop + `text`/`audio`/`per_codebook` returns | `src/training/translation_loss.py:compute_hierarchical_translation_loss` | ✅ the exact hook for Phase C weighting/masking |
| Padding-weighted text loss (Phase 0) | same | ✅ keep `text_padding_weight=0.01` |
| `best_by_val` tracking | `train_hierarchical.py:1464` | ✅ extend to composite metric (Phase D) |
| Per-codebook val acc + diag dashboard | `run_validation` + `_group_grad_diag` | ✅ reuse for monitoring |
| **Unlimited checkpoint retention** | `checkpointing.py` (`keep_last_n<=0`) — landed #9 | ✅ enables post-hoc averaging (Phase B) |
| Config-driven LoRA (`r`/`alpha`/targets/exclude_top) | `src/model/lora_setup.py:apply_lora` | ✅ add `lora_dropout` |
| **Missing:** early stopping, model-weight EMA, checkpoint-averaging util, per-codebook weighting/unmasking, depth-unfreeze lever, SpecAugment | — | ⬅ this PR builds these |

---

## Implementation master table (Phases A–E)

| Phase | Build | Code hook (file:function) | Risk | Tests | DoD |
|---|---|---|---|---|---|
| **A — Regularization** | `lora_dropout`, `label_smoothing`, early-stop, `min_lr_ratio=0`, lr↓/wd↑ | `lora_setup.apply_lora` (+dropout); `translation_loss` (+`label_smoothing` arg → `F.cross_entropy`); `train_hierarchical` early-stop loop; `configs/tpu/stage2_tpu_v6e_v3.yaml` | Low (mostly config + 2 small edits) | label-smoothing ↓ confidence; early-stop fires on rising val | val flat-or-down; early-stop works |
| **B — Weight averaging ⭐** | **`scripts/average_checkpoints.py`** (SWA / greedy-soup / EMA-decay over saved ckpts, CPU); optional in-training EMA | new script (reads `.pt` + `peft_adapter/*.safetensors`); opt. `src/training/ema.py` | Util: Low · in-training EMA: **High** (FSDPv2 sharded) | avg of identical = identical; avg of known tensors = mean; greedy adds only improving | averaged ckpt loads + **beats best-single on val** |
| **C — Deep codebooks** | per-codebook multipliers + **progressive coarse→fine unmask** + staged depth unfreeze | `translation_loss` (cb-weight vector arg replaces `.mean()`); schedule helper in train loop; `lora_setup` depth-unfreeze-after-step lever | Med | cb-weight schedule (K(s) ramp, masked=0); multiplier math | cb0 acc ↑; cb1–7 non-trivial post-unmask |
| **D — Multitask balance** | composite val metric + `text_weight` curriculum/gating | `run_validation` (+composite `0.4·text+0.6·audio`); train-loop schedule/gate | Low | composite calc; curriculum schedule | both streams ↑; selection uses composite |
| **E — Sweep + data + run** | regularization sweep + SpecAugment-on-codes + run | `sweeps/sweep_stage2_v3.yaml`; `promote_sweep_winner` new knobs; `src/data` aug; run scripts | Med | sweep yaml parses; promote maps new knobs; aug shape-preserving | sweep picks generalizing recipe; v0.3 val ↑; release averaged |

---

## Per-phase implementation detail

### Phase A — Regularization (config + 2 edits + early-stop)
- **`configs/tpu/stage2_tpu_v6e_v3.yaml`** (new): `lora.r=16, alpha=32, lora_dropout=0.08`;
  `optim.lr_lora=1e-4`; `train.weight_decay=0.05, grad_clip=1.0`;
  `loss.label_smoothing=0.1, text_padding_weight=0.01`; scheduler `min_lr_ratio=0`,
  `warmup_steps≈8%`.
- **`lora_setup.apply_lora`**: add `lora_dropout: float = 0.0` → `LoraConfig(lora_dropout=...)`; plumb from config.
- **`translation_loss.compute_hierarchical_translation_loss`**: add `label_smoothing: float = 0.0`
  → pass to every `F.cross_entropy(...)` (works with `reduction="none"`).
- **Early stopping** (`train_hierarchical` val block): track `best_composite`, `patience_left`;
  if composite val worsens for `early_stop_patience` (≈4) cycles → break. Reuse `best_by_val` save.

### Phase B — Weight averaging ⭐ (the headline; lowest-risk high-value)
- **`scripts/average_checkpoints.py`** (CPU, torch.load): given a list of checkpoint dirs
  (each = `*.pt` components + `peft_adapter/adapter_model.safetensors`), produce one **averaged**
  checkpoint with the same layout. Modes: `--swa` (uniform last-k), `--soup` (greedy: add a member
  only if it improves a passed val score), `--ema-decay`. Averages **both** the `.pt` tensors and the
  adapter safetensors. Reuses the unlimited checkpoints from #9.
- **🔥 Step-0 probe (do first):** run it on the existing **v0.2** `…/stage2-tpu-v6e-v2-sweepwin/`
  checkpoints (`best_by_val` + 13k/14k/15k) and/or the 8 sweep artifacts → averaged candidate → eval.
  **Zero new TPU time**; may already rescue a better-generalizing model.
- **Optional in-training EMA** (`src/training/ema.py`, flag-gated): shadow copy of trainable params,
  update each step. ⚠️ Under **FSDPv2/SPMD** params are sharded → EMA shadow lives in sharded space,
  adds host-RSS + a compile path. **Defer** behind `ema.enabled=false` until the post-hoc util proves
  the value; post-hoc averaging captures most of the benefit at far lower risk.

### Phase C — Deep-codebook learning (loss hook + freeze lever)
- **`translation_loss`**: replace `audio_loss = per_codebook.mean()` with
  `audio_loss = (per_codebook * cb_weights).sum() / cb_weights.sum().clamp(min=1)`, where
  `cb_weights: [num_cb]` is **passed in** (keeps the loss pure; XLA-traces fine — same unrolled loop).
- **Schedule helper** (train loop): `cb_weights(step)` = per-codebook multipliers (start *gentle*, e.g.
  `[1,1,1.5,1.5,2,2,2,2]`, not the report's aggressive `[1..128]`) **× an unmask mask** that activates
  codebooks `0..K(step)-1`, where `K` ramps `K0→num_cb` over `progressive_unmask_fraction × total_steps`.
- **`lora_setup`** depth lever: `depth_unfreeze_after_step` + `depth_unfreeze_blocks` (low-LR, last
  1–3 blocks) — opt-in; default keep frozen and rely on unmask + A/B first.

### Phase D — Multitask balance
- **`run_validation`**: also return `val/composite = 0.4·val_text + 0.6·val_audio`; use it for
  `best_by_val` + early-stop. Per-stream + per-codebook already logged.
- **`text_weight` curriculum** (train loop): anneal `0.5→0.3` over first ~25% (simple), **or** adaptive
  gating: if a stream's `L_val/L_train > 1.10–1.15`, nudge its weight ×1.05–1.15. Start with the simple
  curriculum; gating is a follow-up.

### Phase E — Sweep redesign, data, run
- **`sweeps/sweep_stage2_v3.yaml`**: sweep **regularization** (`lora_r∈{8,16,32}`, `lora_dropout`,
  `weight_decay`, `lr_lora`) on a **longer proxy** (~2–3k steps), metric = **best early-stopped composite
  val** (not 600-step train audio loss). Extend `promote_sweep_winner.py` PARAM_MAP for the new knobs.
- **SpecAugment-on-codes** (`src/data`, flag-gated): classic SpecAugment is on spectrograms; on discrete
  Mimi codes adapt to **random time-masking** (mask spans of code frames → `SILENCE_TOKEN`) +
  **codebook dropout**. Apply in the collator/dataset; shape-preserving.
- **Run order:** smoke (all levers fire) → regularization sweep → v0.3 run (early-stopped) →
  `average_checkpoints.py` on the run → **release the averaged weights** as `tr-hi-s2st-v0.3`.

---

## Implementation checklist (this PR's commits)

- [ ] **A** `lora_dropout` + `label_smoothing` + early-stop + `v6e_v3.yaml` (+ `min_lr_ratio=0`).
- [ ] **B** `scripts/average_checkpoints.py` (SWA/soup/EMA modes) + **step-0 v0.2 soup probe** + tests.
- [ ] **C** per-codebook `cb_weights` (multipliers × unmask schedule) in loss + train loop; depth-unfreeze lever.
- [ ] **D** composite val metric + `text_weight` curriculum; wire to early-stop/best-by-val.
- [ ] **E** `sweep_stage2_v3.yaml` + promote-mapping + SpecAugment-on-codes; run scripts.
- [ ] Torch-free unit tests: averaging math, cb-weight schedule, composite metric, label-smoothing, promote mapping; all in CI (`tests/`).
- [ ] Keep the **TPU↔GPU seam** (no `torch_xla` in `src/model`/`src/data`); `check_backend_seam.sh` green.

## Run sequence (TPU v6e-8)

1. **Step 0 (CPU/GPU, no TPU):** soup/average the existing v0.2 artifacts → eval → decide if it already wins.
2. **Smoke** (`stage2_tpu_v6e_valfix_smoke`-style on v3 config): confirm dropout, label-smoothing, cb-unmask schedule, early-stop, and the averaging util all fire.
3. **Regularization sweep** (`sweep_stage2_v3.yaml`) → `promote_sweep_winner` → `v6e_v3.yaml`.
4. **v0.3 run**, early-stopped on composite val → `average_checkpoints.py` → release `tr-hi-s2st-v0.3`.

## Open decisions (need owner input)

1. **In-training EMA now, or post-hoc averaging only** for v0.3? (Recommend: post-hoc first.)
2. **Depth-decoder unfreeze** — enable for v0.3, or stay frozen + rely on unmask/A/B?
3. **Data:** keep ~1.18M pairs, or build the synthetic/augmented set before the run?
4. **Compute horizon** — short early-stop (~2–4k) vs. longer-with-averaging.

## References
Rationale + citations live in [`docs/next-v0.3-training-plan.md`](next-v0.3-training-plan.md)
(PR #9): Model Soups / SWA / LAWA / EMA (Phase B), moshi-finetune / SoundStorm / Siren / MusicGen
(Phase C), Unsloth / Raschka / PEFT-survey (Phase A), SpecAugment / SeamlessM4T (Phase E).
